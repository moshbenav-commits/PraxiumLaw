{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "maxDuration": 60
      }
    }
  ],
  "installCommand": "pip install -r requirements.txt",
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ]
}
